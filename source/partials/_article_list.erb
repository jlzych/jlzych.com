<% articles.each_with_index do |article, i| %>
  <p>
    <%= link_to article.title, article %>
    <time class="archives__post-date" datetime="<%= article.date.strftime('%Y-%m-%d') %>">(<%= article.date.strftime('%b %e') %>)</time>
  </p>
<% end %>
